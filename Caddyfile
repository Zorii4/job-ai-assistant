:80 {
	encode zstd gzip

	@api path /health /users* /resumes* /api/auth/*
	handle @api {
		reverse_proxy api:3000 {
			header_up X-Real-IP {remote_host}
			header_up X-Forwarded-Proto {scheme}
			header_up X-Forwarded-Host {host}
		}
	}

	handle {
		try_files {path} /index.html
		file_server
	}
}
